spring:
  application:
    name: micro-service-buy
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/  #这个client服务在注册中心地址注册
server:
  port: 8081
#  instance:
#    hostname: localhost
#    instance-id: http://localhost:8081
